# Core ML libraries
numpy>=1.23.0,<2.0.0      # NumPy 1.23.0 ke atas mendukung Python 3.12
pandas>=2.0.0            # Pandas 2.0.0 ke atas mendukung Python 3.12
scikit-learn>=1.2.0      # Scikit-learn 1.2.0 ke atas umumnya kompatibel
tensorflow==2.16.1       # TensorFlow terbaru yang mendukung Python 3.12 (mungkin ada versi lebih baru saat ini)
opencv-python-headless>=4.6.0.0 # Versi lebih baru untuk kompatibilitas
mediapipe==0.10.11       # Versi terbaru MediaPipe yang mendukung Python 3.12
Pillow>=9.0.0            # Pillow versi lebih baru
joblib>=1.2.0

# Model export libraries
onnx>=1.14.0
onnxruntime>=1.15.0
tf2onnx>=1.10.0
sklearn2onnx>=1.13.0

# Image processing
albumentations>=1.3.0

# Utilities
tqdm>=4.64.0             # Perbaikan penulisan dari tqdma menjadi tqdm
matplotlib>=3.7.0
seaborn>=0.12.0

# Flask
Flask>=2.3.0             # Versi Flask yang lebih baru

# Data handling
pathlib2==2.3.0          # Versi ini mungkin masih berfungsi, atau bisa dihapus jika tidak diperlukan
                         # karena pathlib sudah menjadi bagian dari pustaka standar Python.
